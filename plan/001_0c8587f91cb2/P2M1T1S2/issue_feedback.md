# Implementation Issue Feedback (Attempt 1/3)

The previous implementation attempt encountered an issue that requires re-planning.

## Issue Details

HALTED: the S1 prerequisite deliverable does not exist in the codebase, so this S2 PRP (which is explicitly a MODIFY/DELTA on top of S1's end-state) cannot be executed as written.\n\nTHE PROBLEM, SIMPLY:\nThe PRP I was asked to run (P2.M1.T1.S2) is the second half of a two-part job. It repeatedly says things like 'MODIFIED (not created) from S1's end-state', 'renames Size -> Geometry', 'S2 REPLACES S1's parseGeom tests', and 'S1's _ = @import(\

## Full Agent Output

Definitive. `src/capture.zig` has **never existed** in this repository's git history — no commit, no branch, no stash, no reflog entry touches it. S1 was marked "Complete" in `tasks.json` but was never actually implemented.

Per the instructions, when a fundamental issue with the plan is found, I must halt and produce a thorough explanation. The S2 PRP is explicitly and pervasively a **delta/modify** on S1's end-state, and that foundation does not exist. Implementing S2 as-written is impossible (there is nothing to rename `Size`→`Geometry` from, no S1 tests to replace, no S1 import line to "leave unchanged").

```json
{
   "result": "issue",
   "message": "HALTED: the S1 prerequisite deliverable does not exist in the codebase, so this S2 PRP (which is explicitly a MODIFY/DELTA on top of S1's end-state) cannot be executed as written.\n\nTHE PROBLEM, SIMPLY:\nThe PRP I was asked to run (P2.M1.T1.S2) is the second half of a two-part job. It repeatedly says things like 'MODIFIED (not created) from S1's end-state', 'renames Size -> Geometry', 'S2 REPLACES S1's parseGeom tests', and 'S1's _ = @import(\"capture.zig\") already surfaces S2's tests'. In other words, S2 assumes S1 already happened: that src/capture.zig exists with a Size type, a 2-field parseGeom, a Runner/Backend seam, captureCmd, realRun, a FakeTmux test suite, and that main.zig already imports capture.zig. S2's only job is to edit/extend all of that.\n\nBut S1's output is completely missing:\n- src/capture.zig does NOT exist (ls fails; 'find' finds nothing).\n- main.zig's test block imports only palette.zig, render.zig, and golden_test.zig — there is NO '_ = @import(\"capture.zig\");' line that S1 Task 7 was contracted to add.\n- git history confirms it: 'git log --all -- src/capture.zig' is empty. The file has NEVER existed in any commit, branch, stash, or reflog entry. There is only one branch (main).\n- The only types named Size live in src/render.zig (a separate, unrelated type the PRP explicitly tells me NOT to touch).\n\nTHE PIPELINE STATE CONTRADICTS THE CODE:\nplan/001_0c8587f91cb2/tasks.json marks subtask P2.M1.T1.S1 as status=\"Complete\". But the code S1 was supposed to produce is absent. So the orchestrator believes S1 is done and dispatched S2 on top of it, when in reality S1 was never implemented (or its implementation was lost). This is a pipeline-state corruption: the task status got advanced without the work landing.\n\nWHY I DID NOT JUST 'IMPLEMENT S2 ANYWAY':\nThe S2 PRP is not self-contained. At least 8 of its tasks and success criteria are phrased as edits to existing S1 code (rename an existing Size, add a field to an existing Captured, replace an existing 2-field parseGeom test, leave an existing captureCmd/realRun/Backend 'unchanged', confirm an existing main.zig import 'still' exists). If I improvise, I would actually be doing S1's whole job plus S2's delta in one pass — but doing so blind, without S1's verified research (findings.md for S1 is referenced but the S2 PRP says to read S1's PRP as the authoritative starting point), and I would be mislabeling S1 work as S2. That risks: (a) silently swallowing the real defect (S1's code is missing, but tasks.json says it shipped), which will corrupt every downstream subtask that depends on S1; (b) guessing at S1's exact module structure, which the S2 PRP depends on being byte-precise ('captureCmd/realRun/real()/Backend are byte-for-byte unchanged from S1'); (c) violating the rule that I implement the PRP, not manage the pipeline — marking S1 not-done or re-running S1 is the orchestrator's job, not mine.\n\nWHAT NEEDS TO HAPPEN (for a human/orchestrator, not me):\n1. Re-run P2.M1.T1.S1 so src/capture.zig (with Size, Mode, Captured, Runner, real, geometry, captureCmd, capture, the FakeTmux tests) and the main.zig import line actually land in the tree. Its tasks.json status was set to Complete prematurely.\n2. OR, if S1 is genuinely intended to be skipped/folded-in, the orchestrator must regenerate this S2 PRP as a self-contained CREATE (not MODIFY) spec, because the current PRP's contract is built entirely on top of S1's end-state.\n3. After S1's code is actually present, re-dispatch P2.M1.T1.S2 and it will apply cleanly as the delta it was designed to be.\n\nI made NO changes to the repo. No src/, tests/, lib/ files were created or modified. plan/ and tasks.json were left untouched."
}
```

## Instructions

Review this feedback and create a revised PRP that addresses the issue.
If the issue is fundamentally impossible to resolve, output `"result": "fail"` with an explanation.
